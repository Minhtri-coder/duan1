<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền nút -->
    <solid android:color="#1A9E88" />

    <!-- Bo góc -->
    <corners android:radius="12dp" />

    <!-- Viền (nếu muốn có) -->
 <stroke android:width="1dp" android:color="#1A9E88" />

    <!-- Khoảng cách bên trong -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>
